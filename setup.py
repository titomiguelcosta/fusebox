from setuptools import find_packages, setup

setup(
    name="fusebox",
    version="0.1",
    description="Fusebox -  Music Recommendation System",
    platforms=["linux"],
    packages=find_packages()
)
